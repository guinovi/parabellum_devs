extends layout

block content
    section.page-hero
        p.eyebrow Seguridad
        h1 Acceso denegado
        p No tenés permisos para acceder a esta sección.
        .actions
            a.btn.btn-primary(href='/') Volver al inicio
